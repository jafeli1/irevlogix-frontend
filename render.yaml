services:
  - type: web
    name: irevlogix-frontend
    env: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    domains:
      - irevlogix.ai
      - www.irevlogix.ai
